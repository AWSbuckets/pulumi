name: dash-app
description: A minimal Python Pulumi program
program: pulumi/aws_stack.py
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: python
  aws:region:
    value: "eu-west-2"
